# 黑棋 / 白棋 AI 算法说明

> 条件判断用缩进表示；同一层缩进为顺序执行，缩进更深表示条件成立后执行。

---

## 一、通用概念

- 实心圆：黑棋落子后直接形成五连的空格。优先级最高。
- 三角形：黑棋落子后形成活四或四三的空格。
- 大圆圈：黑棋落子后形成冲四或活三的空格。
- 小圆圈：黑棋落子后形成眠三或活二/眠二的空格。
- 红点：黑棋落子后形成眠二/眠一的空格。
- 空心三角形：三角形攻势中，若白棋吃掉某个只剩一气的黑棋块后能消除全部实心圆和三角形，则该三角形为空心三角形。
- 蓝叉：黑棋禁手或无气位置。
- 白棋领地：无法参与黑棋连五的空格，以及无气位置。
- 黑棋块：横竖相连的黑棋块。
- 气：黑棋块相邻的空格数量。
- 关联：黑棋块消失后，会导致某个红色位置消失，则称该黑棋块与该红色位置关联。
- 搜索深度：`minimax 深度 + 2`。

---

## 二、白棋 AI

```
若 存在实心圆或三角形：
    候选点 = 实心圆位置 + 三角形位置

    对每个实心圆/三角形位置：
        假设黑棋落在这个位置
        找到这个位置所在的黑棋块
        若 这个位置是实心圆：
            若 这个黑棋块只有 1 气：
                把这口气加入候选点
        若 这个位置是三角形：
            若 这个黑棋块只有 1 气或 2 气：
                把这个黑棋块的所有气加入候选点

    对每个实心圆/三角形位置，继续遍历其攻势方向：
        对每个方向：
            若 该方向是连五/活四/冲四：
                找到该方向上的黑棋块
                若 黑棋块只有 1 气：
                    把这口气加入候选点
            若 该方向是活三：
                找到该方向上的黑棋块
                若 黑棋块只有 1 气或 2 气：
                    把这个黑棋块的所有气加入候选点

    筛选候选点：
        对每个候选点：
            假设白棋落在这个候选点
            检查：
                是否消除所有实心圆
                若 没有消除所有实心圆：
                    这个候选点不安全
                若 消除所有实心圆：
                    继续检查黑棋的三角形和大圆圈：
                        对每个三角形/大圆圈：
                            假设黑棋落在这个位置
                            若 黑棋落子后形成实心圆或三角形：
                                递归调用白棋算法，继续判断是否能消除全部实心圆
                            若 不能消除：
                                这个候选点不安全
            若 所有黑棋应对都能被白棋消除：
                这个候选点安全

    若 没有任何安全候选点：
        记录黑棋获胜过程
        白棋投子认负
    若 只有 1 个安全候选点：
        直接落子
    若 有多个安全候选点：
        用深度 0 的 minimax 选择最优位置落子

若 不存在实心圆或三角形：
    若 存在大圆圈：
        选中的红色位置 = 大圆圈
    若 没有大圆圈，但存在小圆圈：
        选中的红色位置 = 小圆圈
    若 没有大圆圈，也没有小圆圈，但存在红点：
        选中的红色位置 = 红点
    若 不存在任何红色位置：
        选择横竖斜方向上，与棋子、白棋领地、棋盘边缘距离最大的空格落子

    候选点 = 选中的红色位置

    对每个选中的红色位置：
        找到与其关联的黑棋块
        若 黑棋块的气数 <= B：
            把这个黑棋块的所有气加入候选点

        B 的定义：
            红色位置落子后形成冲四/活四：B = 2
            红色位置落子后形成活三/眠三：B = 3
            红色位置落子后形成活二/眠二：B = 4
            即：对活 n/眠 n 位置，B = 6 - n

    排除白棋领地
    加上白棋领地中，紧邻的黑棋块不是整块都是白棋领地的位置

    若 候选点只有 1 个：
        直接落子
    若 候选点有多个：
        用 minimax 选择最优位置落子
```

---

## 三、黑棋 AI

```
若 存在实心圆：
    直接落子实心圆

若 不存在实心圆，但存在三角形：
    执行算法 A：

    候选点 = 三角形位置

    加上白棋能吃掉的黑棋攻势块的气：
        对每个三角形位置：
            找到组成攻势的黑棋块
            若 这个黑棋块只有 1 气：
                把这个气加入候选点
        筛选：
            假设黑棋落在这个气上
            若 落子后新的黑棋块气数 > 1：
                保留
            若 落子后新的黑棋块气数 <= 1：
                排除

    若 候选点只有 1 个：
        直接落子

    对每个候选点：
        假设黑棋落在这个候选点

        若 黑棋落子后直接连五：
            直接落子这个候选点

        调用白棋算法

        若 白棋算法返回“投子认负”：
            直接落子这个候选点
            记录黑棋必胜过程
            关闭黑棋普通搜索
            以后黑棋按必胜表落子

        若 白棋算法返回一个落子点位集合：
            对每个白棋落子点：
                假设白棋落在这个点
                若 之后仍存在三角形：
                    递归调用算法 A
                    若 递归找不到使白棋投子认负的点：
                        这个黑棋候选点不能保证必胜
                若 之后不存在三角形：
                    停止这个分支的搜索
                    这个黑棋候选点不能保证必胜

    若 只剩 1 个候选点：
        直接落子
    若 有多个候选点：
        用深度 0 的 minimax 选择最优位置落子

若 不存在实心圆，也不存在三角形：
    若 存在大圆圈：
        选中的红色位置 = 大圆圈
    若 没有大圆圈，但存在小圆圈：
        选中的红色位置 = 小圆圈
    若 没有大圆圈，也没有小圆圈，但存在红点：
        选中的红色位置 = 红点
    若 不存在任何红色位置：
        选择横竖斜方向上，与棋子、白棋领地、棋盘边缘距离最大的空格落子

    候选点 = 选中的红色位置

    对每个选中的红色位置：
        找到与其关联的黑棋块
        若 黑棋块的气数 <= B：
            把这个黑棋块的所有气加入候选点

        B 的定义：
            红色位置落子后形成冲四/活四：B = 2
            红色位置落子后形成活三/眠三：B = 3
            红色位置落子后形成活二/眠二：B = 4
            即：对活 n/眠 n 位置，B = 6 - n

    排除白棋领地
    排除蓝叉
    排除“黑棋落子后只剩一气且未连五”的位置
    加上白棋领地中，紧邻的黑棋块不是整块都是白棋领地的位置

    若 候选点只有 1 个：
        直接落子
    若 候选点有多个：
        用 minimax 选择最优位置落子
```

---

## 四、minimax

```
评分对象：空格 + 黑棋

空格评分：
    实心圆：0 分，因为已在最高优先级处理
    普通三角形：10000 分
    空心三角形：500 分
    大圆圈：625 分
    小圆圈：25 分
    红点：1 分
    白棋领地：-1 分
    黑棋领地：-1 分

黑棋块评分：
    黑棋块分数 = A * (1 - (1/√2)^n)
    其中：
        A = 假设这个黑棋块瞬间消失后，减少的红色位置分数
        n = 这个黑棋块的气数

黑棋目标：
    使局面分数尽可能高

白棋目标：
    使局面分数尽可能低

搜索：
    从 0 层开始，逐层搜索 0、2、4、6、8 层
    每完成一层，记录该层最优位置
    若 搜索达到设定层数 且 达到最短搜索时间：
        停止搜索
    若 达到最长搜索时间：
        立即停止当前层搜索
        使用上一个已完成层的最优位置落子
    若 点击“AI 立即落子”：
        立即停止当前层搜索
        使用上一个已完成层的最优位置落子
    若 搜索中被中断，且当前层未完成：
        忽略当前层未完成部分

Alpha-beta：
    可以使用无损剪枝
    黑棋层维护下界 alpha
    白棋层维护上界 beta
    若 beta <= alpha：
        剪枝
```

---

## 五、缓存与增量更新

黑棋块缓存保存：
- 组成黑棋坐标列表
- 黑棋块分数
- 气的位置列表
- 气数
- 关联最高威胁级别

测试落子时只更新：
- 横竖斜方向 4 格内的空格状态
- 紧邻黑棋块状态
- 黑棋块合并后的净分数：
  - 新黑棋块分数 - 原来几个黑棋块分数之和

---

## 六、空心三角形判定

```
对每个三角形位置：
    假设黑棋落在这个三角形
    遍历这个三角形攻势中的黑棋块
    若 某个黑棋块只有 1 气：
        假设白棋在这口气落子并吃掉这个黑棋块
        若 吃掉后所有实心圆和三角形都消失：
            这个三角形是空心三角形
            分数从 3125 改为 500
```
