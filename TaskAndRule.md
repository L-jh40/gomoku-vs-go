
gomoku-vs-go1里的代码是一个部分失败的尝试，可以参考其思路，编写一个新文件夹，装下全部新写的代码。
请严格按照以下算法规格实现代码，不要做任何近似、简化或优化，除非我明确要求。若会导致性能问题，先询问。未经我的许可，绝对禁止将精确计算替换为近似值、估计值、启发式或简化公式。一切未清晰表述的部分都需要询问，一切实现方法都需要用文字说明（如描述中说“距离”，选择暂时用切比雪夫距离实现后，输出文字明确询问需要什么距离，得到“横竖斜按格子算距离”的回答后，进行修改），禁止为了对话和谐而隐瞒给出算法和实际实现的区别。理解输入的提示词时，注意分段。每个双换行、换行、句号（表示一块判断结束，如if-else结束后恢复缩进）、分号（表示if-else等）都承担不同的分段作用。
询问一般直接提出选择，如果是可以简单修改的程序（如只改一个参数、一个函数）可以先做，最后输出时询问是否改。

游戏规则：黑棋使用只考虑禁手的比赛五子棋规则（无指定开局、三手交换和五手两打，有三三（黑棋一子落下同时形成两个或两个以上的活三，此子必须为两个活三共同的构成子）、四四（黑棋一子落下同时形成两个或两个以上的冲四或活四）、长连（形成的 6 个或 6 个以上同色棋子不间隔的相连）共三种禁手），以形成的 5 个同色棋子不间隔的相连获胜。白棋使用围棋规则可以吃黑棋，默认以堵住所有黑棋连五路径获胜。

黑棋无法落子禁手位置、无气位置（落子后无气，即围棋中的“自吃禁止”位置），AI搜索范围为空格排除“白棋领地”（无法参与连五的点位，以及无气位置，显示灰色小方格在上层。领地必不是红色状态）、落子后只剩一气且为未连五的位置，然后加上白棋领地中，紧邻黑棋块不全为白棋领地的位置（可能可以用于长气）。白棋可以在所有空格落子，AI搜索范围先排除“白棋领地”、无气位置，然后加上这些排除的位置中，紧邻黑棋块不全为白棋领地的位置（可能可以用于紧气/吃子）。

白棋AI：有实心圆或三角形时，候选点为：1.实心圆或三角形；2.吃掉组成攻势的黑棋的位置（即遍历组成活四、冲四、活三的黑棋所在黑棋块中，若黑棋块只有1气，将气的所在空格加入候选点）。筛选出落子后能确保消除所有实心圆的候选点，若没有，记录黑棋获胜路径以备复盘，投子认负（认负后，弹出和黑棋投子认负一样大小的窗口，在“确定”右边加按钮“显示黑棋获胜路径”，点击后进入复盘，新落下的棋子及边框变小，黑棋不搜索，通过查表落子。如A1111B，AB是空格，1是黑棋，记录：若白棋落子A，则黑棋落子B；否则落子A。需要注意玩家可能不在白棋搜索点落子，此时任意走一条路径即可。复盘结束只有黑棋获胜的一种可能，若结束后未获胜，弹出报错窗口）。然后遍历三角形和大圆圈，检查在这里落子黑棋能否形成实心圆或三角形，若能，运用同样算法继续搜索判断哪些点位能消除所有实心圆；若不能，则停止搜索，剩余候选点为落子点位集合。达到搜索深度等于minimax深度+2时，停止搜索，若有多个落子点位，开始深度为0的Minimax选取最优点位落子。只剩一个候选点时也停止搜索，直接落子。
若没有实心圆和三角形，在红色位置开始minimax。
若没有红色位置，选择在横竖斜线上与棋子、白棋领地和棋盘边缘的距离最大的空格。

黑棋AI：有实心圆直接落子实心圆。
在无实心圆、有三角形时，执行算法A：
落子点位集合先定义等于候选点集合等于：1.三角形；2.白棋能吃掉组成攻势的黑棋的位置（需要筛选出：落子后能使新黑棋块的气大于1的）。遍历每个候选点，落子一个候选点后，直接调用白棋算法：
1.若返回None（即投子认负），则直接落子这个候选点，接收白棋算法返回的黑棋必胜路径，关闭AI，后面只做查表保证必胜；
2.否则白棋返回一个落子点位集合，然后遍历每个点位让白棋落子：
2.1.若有三角形，重复相同步骤（算法A）搜索可以使白棋投子认负的点位，并把没有三角形的候选点排除出落子点位集合；
2.2.若没有三角形，停止搜索。
达到搜索深度等于minimax深度+2时，停止搜索，若有多个落子点位，开始深度为0的Minimax选取最优点位落子。只剩一个候选点时也停止搜索，直接落子。

minimax算法：对空格和黑棋计分：实心圆（黑棋连五点位。不计分，因为在算法最高优先级处理）-三角形（落子后形成黑棋(活三+冲四)或形成活四的点位。625分）-大圆圈（落子后形成黑棋冲四或活三的空格。125分）-小圆圈（落子后形成黑棋眠三或眠二的空格。25分）-红点（落子后形成黑棋眠二的空格。1分）-领地（无法参与黑棋连五的空格。-1分），黑棋领地也算-1分。黑棋块分数=A(1-(1/2)^n)，其中A为假设黑棋块瞬间消失减少的分数，n为黑棋块的气数。需要实现：蓝叉（禁手与无气位置）和领地均会覆盖红色位置，减少得分。黑棋使分数尽可能高，白棋使分数尽可能低。可以加入alpha-beta无损剪枝。实际运行时，不需要全局算分，先缓存所有黑棋块（记录信息：包含的黑棋、气的位置、分数），测试落子时只更新：横竖斜方向4格的空格（判断是不是领地，然后是不是、是哪种红色位置）/黑棋（是不是领地）状态、紧邻黑棋块状态（更新黑棋块信息，根据消失后影响的红色位置后气数计算得分。若黑棋落子将多个黑棋块连为一个，净得分等于新黑棋块的分数减原来的分数总和）
